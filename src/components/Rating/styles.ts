import styled from 'styled-components'
import { cores } from '../../styles'

export const RatingContainer = styled.div`
  color: ${cores.laranja};
  font-size: 18px;
  font-weight: 700;
  padding: 4px 6px;
  display: flex;
  margin-right: 8px;
  margin-left: 409px;
  position: absolute;
  img {
    margin-left: 8px;
  }
`
